const notas = [10, 6, 8];
//  push -> adicionar um elemento dentro do array
notas.push(7)
// adicionar elemento dentro da função com o indice numerado na sequencia
const media = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length;
console.log(media);